<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base theme; Compose drives the real colours. Keeps a Material3 splash backdrop. -->
    <style name="Theme.BritSpeak" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@color/brand_indigo</item>
        <item name="android:windowBackground">@color/surface</item>
        <item name="android:navigationBarColor">@color/surface</item>
    </style>
</resources>
